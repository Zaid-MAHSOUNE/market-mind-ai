TASK: Resolve the company name "{query}" into a valid financial ticker.

### HIERARCHY RULES:
1. FRENCH ASSETS: Use Euronext Paris (suffix '.PA').
2. PARENT MAPPING: If the entity is private (e.g., Enedis, SpaceX), return the ticker of the majority owner (e.g., 'EDF', 'DISH').
3. STATE-OWNED: If no ticker exists, return "PRIVATE_ENTITY".

RETURN FORMAT: [TICKER] (Only the symbol, e.g., AAPL or MC.PA)